name: semgrep

on:
  pull_request:
  merge_group:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  semgrep:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      # Semgrep version policy: use the latest CLI intentionally so security rules and engine updates land quickly.
      # Owners review monthly; pin during reproducibility incidents or when upstream changes break CI.
      - run: python -m pip install semgrep
      - run: semgrep scan --config p/default --error
